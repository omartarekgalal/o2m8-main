version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
